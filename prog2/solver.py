#TODO
    main()
        heap push root
        while not solved
            k++
            while depth < k
                if solved() return program
                else
                    pop heap
                    depth = just popped cost
                    explore children()

    explore_children()
        for each child:
            compute heuristic()
            push onto heap
        return heap to main

    compute heuristic()
        antons method?

    solved()
        if h == 0 then solved
